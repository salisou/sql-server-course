# Cheatsheet

SELECT/FROM/WHERE/GROUP BY/HAVING/ORDER BY; INSERT/UPDATE/DELETE; INNER/LEFT/RIGHT/FULL JOIN; COUNT/SUM/AVG/MIN/MAX; IS NULL/ISNULL/COALESCE; WITH; VIEW; PROCEDURE; BEGIN TRAN/COMMIT/ROLLBACK; TRY/CATCH/THROW; CREATE INDEX.